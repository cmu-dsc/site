---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Poker AI - Coming Soon - CMU Data Science Club">
  <main class="min-h-screen flex flex-col items-center justify-center p-8">
    <div class="max-w-2xl mx-auto text-center">
      <div class="mb-8 relative">
        <div class="relative inline-block">
          <svg
            class="w-32 h-32 text-[#082b57]/20"
            fill="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              d="M17 4a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h10zm-.5 2h-9a.5.5 0 0 0-.5.5v11a.5.5 0 0 0 .5.5h9a.5.5 0 0 0 .5-.5v-11a.5.5 0 0 0-.5-.5zM8 9h8v2H8V9zm0 4h8v2H8v-2z"
            ></path>
          </svg>
          <div class="absolute inset-0 flex items-center justify-center">
            <span class="text-4xl">♠️</span>
          </div>
        </div>
      </div>

      <h1 class="text-4xl md:text-5xl font-bold mb-4 text-[#082b57]">
        Coming Soon
      </h1>

      <p class="text-lg md:text-xl mb-8 text-[#082b57]/80">
        Our Poker AI competition is currently under development. Stay tuned for
        an exciting opportunity to test your skills building bots!
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/"
          class="inline-flex items-center justify-center px-6 py-2 rounded-full bg-[#082b57] text-white hover:bg-[#082b57]/90 transition-colors"
        >
          Return Home
        </a>
        <a
          href="/discord"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center px-6 py-2 rounded-full bg-[#082b57]/10 text-[#082b57] hover:bg-[#082b57]/20 transition-colors"
        >
          Join Discord for Updates
        </a>
      </div>
    </div>
  </main>

  <style>
    /* Add glass effect to the main container */
    main {
      position: relative;
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
    }

    /* Animated gradient border */
    .relative::before {
      content: "";
      position: absolute;
      inset: -2px;
      border-radius: 1rem;
      padding: 2px;
      background: linear-gradient(
        45deg,
        rgba(8, 43, 87, 0.3),
        rgba(8, 43, 87, 0.1)
      );
      -webkit-mask:
        linear-gradient(#fff 0 0) content-box,
        linear-gradient(#fff 0 0);
      mask:
        linear-gradient(#fff 0 0) content-box,
        linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      mask-composite: exclude;
      pointer-events: none;
    }
  </style>
</Layout>
